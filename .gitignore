# Xcode user settings
xcuserdata/
*.xcscmblueprint
*.xccheckout

# xcodegen — commit project.yml, not the generated .xcodeproj.
# The project is a build artifact (regenerate with `cd app && xcodegen generate`);
# keeping it untracked avoids signing-team churn and non-deterministic diffs.
GameScoring.xcodeproj/

# Build artifacts
.build/
*.ipa
*.dSYM
*.dSYM.zip

# Swift Package Manager
.swiftpm/
Package.resolved

# macOS
.DS_Store

# Playgrounds
timeline.xctimeline
playground.xcworkspace
